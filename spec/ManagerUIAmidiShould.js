'use strict';

let should = require('chai').should();

describe('ManagerUIAmidi Should', () => {

    /*
    Es necesario el uso de dobles de pruebas a la espera de ponerme con dobles
     beforeEach(() => {
     passwordValidator = passwordValidator();
     });
     */

    it("test green",() => {
        true.should.equal(true);
    });

    xit("Al Start screen of load is visible",() => {

    });

});