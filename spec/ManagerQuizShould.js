'use strict';

let should = require('chai').should();

describe('ManagerQuiz Should', () => {

        /*
        beforeEach(() => {
            passwordValidator = passwordValidator();
        });
        */

        xit("",() => {
        passwordValidator.validate("12345678A%").should.equal(true);
        });

});